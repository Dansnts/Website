services:
  nginx:
    build: .
    container_name: nginx-www
    ports:
      - "8080:80"
    volumes:
      - ./www:/www:ro       # Monte le dossier www local dans le conteneur
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
