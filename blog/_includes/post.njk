---
layout: base.njk
---
<article class="prose">
  <h1>{{ title }}</h1>
  <div class="post-meta">
    <span>{{ date | dateStr }}</span>
    {% if tags %}<span>{{ tags | join(", ") }}</span>{% endif %}
  </div>
  {{ content | safe }}
</article>
